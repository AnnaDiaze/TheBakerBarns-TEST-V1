<!DOCTYPE html>
<html>
<head>
    <%- include('partials/header1.ejs') %>
    <style>
        .center-message {
            text-align: center; /* horizontal centering */
            margin: 30px 0;     /* spacing below header */
        }

        .center-message h2 {
            color: #333;
            margin-bottom: 15px;
        }

        .center-message p {
            color: #555;
            margin-bottom: 20px;
        }

        .center-message a button {
            margin: 0 10px;
            padding: 10px 20px;
            border: none;
            border-radius: 6px;
            background-color: #ff8c00;
            color: #fff;
            font-size: 16px;
            cursor: pointer;
            transition: background-color 0.2s ease;
        }

        .center-message a button:hover {
            background-color: #e67600;
        }
    </style>
</head>
<body>
  <header>
    <h1>The Baker Barns Bakery</h1>
    <%- include('partials/navbar1.ejs') %>
  </header>

  <!-- Centered login message -->
  <div class="center-message">
      <h2>You need to log in to checkout</h2>
      <p>Only registered users can place orders, including customized cakes.</p>
      <a href="/login"><button>Login Now</button></a>
      <a href="/products"><button>Continue Browsing</button></a>
  </div>


</body>
</html>
