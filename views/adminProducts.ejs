<html>
<head>
    <%- include('partials/header1.ejs') %>
</head>
<body>
  <header>
    <h1>The Baker Barns Bakery</h1>
    <%- include('partials/navbar1.ejs') %>
  </header>

      <style>
    table {
      width: 100%;
      border-collapse: collapse;
      margin-top: 20px;
    }
    th, td {
      padding: 12px;
      border: 1px solid #ccc;
      text-align: center;
    }
    th {
      background: #f4f4f4;
    }
    form {
      margin: 0;
    }
    select {
      padding: 5px;
    }
    button {
      padding: 6px 12px;
      margin-left: 5px;
      background: #4CAF50;
      color: white;
      border: none;
      cursor: pointer;
    }
    button:hover {
      background: #45a049;
    }
    .back-link:hover { background: #45a049; }

    .btn-update {
      background: #27ae60;
      color: white;
    }

    .btn-delete {
      background: #e74c3c;
      color: white;
    }
  </style>
</body>

  <!-- Admin Action Bar -->
  <div class="d-flex flex-wrap p-2 bg-light border-bottom">
    <a href="/adminonly" class="btn btn-outline-dark m-1">Admin Panel</a>
    <a href="/admin/Orders" class="btn btn-outline-dark m-1">Manage Orders</a>
    <a href="/admin/Products" class="btn btn-outline-dark m-1">Manage Products</a>
    <a href="/admin/Recipes" class="btn btn-outline-dark m-1">Manage Recipes</a>
    <a href="/admin/Users" class="btn btn-outline-dark m-1">Manage Users</a>
  </div>

<h1>Manage Products</h1>

<!-- Add new product -->
<form action="/admin/products/add" method="POST">
  <input type="text" name="name" placeholder="Product Name" required />
  <textarea name="description" placeholder="Description"></textarea>
  <input type="number" name="price" placeholder="Price" step="0.01" min="0" required />
  <input type="number" name="stock" placeholder="Stock" step="0.01" min="0" required />
  <input type="number" name="category_id" placeholder="Category" step="0.01" min="0" required />
  <button type="submit" class="btn btn-update">Add Product</button>
</form>
<hr>

<!-- List existing products -->
<h3>All Products</h3>
<table>
  <tr>
    <th>Name</th><th>Description</th><th>Price</th><th>Stock</th><th>Category</th><th>Update</th><th>Delete</th>
  </tr>
  <% products.forEach(product => { %>
    <tr>
      <form action="/admin/products/update/<%= product.id %>" method="POST">
        <td><input type="text" name="Name" value="<%= product.name %>"></td>
        <td><textarea class="description-box">"<%= product.description %>"</textarea></td>
        <td><input type="number" name="Price" value="<%= product.price %>" step="0.01" min="0"></td>
        <td><input type = "number" name="Stock" value="<%= product.stock %>"></td>
        <td><input type = "number" name="Category" value="<%= product.category_id %>"></td>
        <td><button type="submit" class="btn btn-update">Update</button></td>
      </form>
      <form action="/admin/products/delete/<%= product.id %>" method="POST" style="display:inline;">
        <td><button type="submit" class="btn btn-update">Delete</button></td>
      </form>
    </tr>
  <% }) %>
</table>
<a href="/adminOnly" class="btn btn-update">← Back </a>
</html>