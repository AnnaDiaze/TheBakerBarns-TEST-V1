<html>
<head>
  <%- include('partials/header1.ejs') %>
  <style>
    :root{--bb-accent:#ff4d6d;--bb-cream:#fff3f6;--bb-dark:#111}
    /* Full‑width layout helpers */
    body{margin:0}
    header,footer,section{width:100%}
    .bb-shell{padding:0 5vw} /* gentle side gutters so content "breathes" while staying full width */

    /* Header + Footer keep your existing partials, just styled to span */
    header.bb{border-bottom:1px solid #eee}
    footer.bb{border-top:1px solid #eee;color:#888}

    /* Hero goes truly edge‑to‑edge */
    .hero{position:relative;min-height:70vh;display:flex;align-items:center;color:#fff;overflow:hidden}
    .hero-bg{position:absolute;inset:0;background:#111 url('/images/home/hero.jpg') center/cover no-repeat}
    .hero::after{content:"";position:absolute;inset:0;background:linear-gradient(180deg,rgba(0,0,0,.35),rgba(0,0,0,.55))}
    .hero .content{position:relative;z-index:2}
    .bb-badge{display:inline-block;background:var(--bb-accent);color:#fff;border-radius:999px;padding:.45rem .85rem;font-size:.9rem}
    .btn{display:inline-block;padding:.8rem 1.2rem;border-radius:.6rem;border:1px solid #fff;background:#fff;color:#000;text-decoration:none;font-weight:600}
    .btn.outline{background:transparent;color:#fff}

    /* Sections */
    .bb-section{padding:4rem 0}
    h1,h2,h3{margin:0 0 .6rem}
    p{margin:.25rem 0 1rem}

    /* Card grid (no Bootstrap needed) */
    .grid{display:grid;gap:1rem}
    .grid.cols-3{grid-template-columns:repeat(3,minmax(0,1fr))}
    .grid.cols-4{grid-template-columns:repeat(4,minmax(0,1fr))}
    @media (max-width:1200px){.grid.cols-4{grid-template-columns:repeat(3,1fr)}}
    @media (max-width:900px){.grid.cols-3,.grid.cols-4{grid-template-columns:repeat(2,1fr)}}
    @media (max-width:560px){.grid.cols-3,.grid.cols-4{grid-template-columns:1fr}}

    .card{border:1px solid #eee;border-radius:1rem;overflow:hidden;background:#fff;box-shadow:0 6px 18px rgba(0,0,0,.05)}
    .card img{width:100%;height:240px;object-fit:cover}
    .card .pad{padding:1rem}
    .price{font-weight:700}

    .bb-why{background:var(--bb-cream)}
    .why-item{background:#fff;border:1px solid #eee;border-radius:1rem;padding:1rem;display:flex;gap:.75rem;align-items:flex-start}
    .why-ico{font-size:1.5rem}

    .cta{background:linear-gradient(135deg,var(--bb-accent),#ff8fab);border-radius:1.25rem;color:#fff}
    .cta .btn{border:0}

    .quote{font-style:italic}
  </style>
</head>
<body>
  <!-- HEADER (full width) -->
  <header>
    <div>
      <a><h1>The Baker Barns Bakery</h1></a>
      <%- include('partials/navbar1.ejs') %>
    </div>
  </header>

  <!-- HERO (edge to edge) -->
  <section class="hero">
    <div class="hero-bg"></div>
    <div class="bb-shell content">
      <span class="bb-badge">Baked fresh daily</span>
      <h1 style="font-size:clamp(2rem,5vw,3.2rem);margin-top:.75rem">Cakes that make every moment sweet</h1>
      <p style="max-width:60ch">Order customer favourites or design your own custom cake in minutes.</p>
      <div style="display:flex;gap:.75rem;flex-wrap:wrap">
        <a href="/products" class="btn">Shop Cakes</a>
        <a href="/custom_cake" class="btn outline">Build a Custom Cake</a>
      </div>
    </div>
  </section>

  <!-- BEST SELLERS (full width with side gutters) -->
  <section class="bb-section">
    <div class="bb-shell">
      <h2 style="font-size:1.8rem">Best Sellers</h2>
      <div class="grid cols-4" style="margin-top:1rem">
        <% const bestSellers = [
          {title:'Classic Chocolate', img:'/images/home/chocolate.jpg', price:'$55', href:'/products?search=chocolate'},
          {title:'Red Velvet', img:'/images/home/red-velvet.jpg', price:'$65', href:'/products?search=red%20velvet'},
          {title:'Ribbon Pound', img:'/images/home/ribbon.jpg', price:'$35', href:'/products?search=ribbon'},
          {title:'Black Forest', img:'/images/home/black-forest.jpg', price:'$55', href:'/products?search=black%20forest'},
          {title:'Lemon Drizzle', img:'/images/home/lemon.jpg', price:'$35', href:'/products?search=lemon'},
          {title:'Vanilla Dream', img:'/images/home/vanilla.jpg', price:'$45', href:'/products?search=vanilla'},
          {title:'Caramel Crunch', img:'/images/home/caramel.jpg', price:'$55', href:'/products?search=caramel'},
          {title:'Tiramisu', img:'/images/home/tiramisu.jpg', price:'$65', href:'/products?search=tiramisu'}
        ]; %>
        <% bestSellers.forEach(item => { %>
          <a class="card" href="<%= item.href %>" style="text-decoration:none;color:inherit">
            <img src="<%= item.img %>" alt="<%= item.title %>" loading="lazy">
            <div class="pad">
              <h3 style="font-size:1rem;margin-bottom:.25rem"><%= item.title %></h3>
              <div class="price"><%= item.price %></div>
            </div>
          </a>
        <% }) %>
      </div>
    </div>
  </section>

  <!-- WHY CHOOSE US -->
  <section class="bb-section bb-why">
    <div class="bb-shell">
      <h2 style="font-size:1.8rem">Why choose Baker Barns?</h2>
      <div class="grid cols-4" style="margin-top:1rem">
        <div class="why-item"><div class="why-ico">🎂</div><div><div style="font-weight:600">Made Fresh Daily</div><small>Baked in small batches for peak flavour.</small></div></div>
        <div class="why-item"><div class="why-ico">🧁</div><div><div style="font-weight:600">Custom Designs</div><small>Create a cake that’s uniquely you.</small></div></div>
        <div class="why-item"><div class="why-ico">🚚</div><div><div style="font-weight:600">Same‑Day Pickup</div><small>Order by 11am for same‑day options.</small></div></div>
        <div class="why-item"><div class="why-ico">⭐</div><div><div style="font-weight:600">Loved Locally</div><small>4.8★ average from happy customers.</small></div></div>
      </div>
    </div>
  </section>

  <!-- CUSTOM CAKE CTA (full width) -->
  <section class="bb-section">
    <div class="bb-shell">
      <div class="cta" style="padding:2rem;display:flex;gap:1rem;align-items:center;justify-content:space-between;flex-wrap:wrap">
        <div>
          <h2 style="margin-bottom:.25rem">Got a celebration coming up?</h2>
          <p style="margin:0">Design a custom cake—choose flavours, layers, frosting, toppers and more.</p>
        </div>
        <a href="/custom_cake" class="btn">Start Customising</a>
      </div>
    </div>
  </section>

  <!-- TESTIMONIALS -->
  <section class="bb-section">
    <div class="bb-shell">
      <h2 style="font-size:1.8rem">Customer testimonials</h2>
      <div class="grid cols-3" style="margin-top:1rem">
        <% const quotes = [
          {name:'Emily R.', text:"Best cake I've ever had!"},
          {name:'Jason M.', text:'Perfect cookies every time'},
          {name:'Priya T.', text:'Lovely people and even better cakes!'}
        ]; %>
        <% quotes.forEach(q => { %>
          <div class="card"><div class="pad"><p class="quote">“<%= q.text %>”</p><div style="font-weight:600">— <%= q.name %></div></div></div>
        <% }) %>
      </div>
    </div>
  </section>

  <!-- FOOTER (full width) -->
  <footer>
    <div>
      <div>© 2025 The Baker Barns Bakery</div>
      <!-- <nav style="display:flex;gap:1rem"><a href="/about">About</a><a href="/recipes">Recipes</a><a href="/contact">Contact</a><a href="/adminonly">Admin</a></nav> -->
    </div>
  </footer>
</body>
</html>
